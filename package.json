{
  "name": "streakers-app",
  "engines": {
    "node": "6.3.1",
    "yarn": "1.6.0"
  },
  "scripts": {
    "build": "yarn --cwd client install && yarn --cwd client build && cd ..",
    "deploy": "cp -a react-client/build/. public/",
    "heroku-postbuild": "yarn build && yarn deploy && echo 'Client built!'"
  }
}
