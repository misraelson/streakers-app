<%= form_with(model: streak, local: true) do |form| %>
  <% if streak.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(streak.errors.count, "error") %> prohibited this streak from being saved:</h2>

      <ul>
      <% streak.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :current_streak %>
    <%= form.number_field :current_streak %>
  </div>

  <div class="field">
    <%= form.label :start_date %>
    <%= form.date_select :start_date %>
  </div>

  <div class="field">
    <%= form.label :end_date %>
    <%= form.date_select :end_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
